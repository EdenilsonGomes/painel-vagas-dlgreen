'use strict';
const form=document.querySelector('#loginForm');const button=document.querySelector('#loginButton');const errorBox=document.querySelector('#loginError');const password=document.querySelector('#senha');document.querySelector('#togglePassword').addEventListener('click',()=>{const show=password.type==='password';password.type=show?'text':'password';document.querySelector('#togglePassword').textContent=show?'Ocultar':'Mostrar';});form.addEventListener('submit',async(event)=>{event.preventDefault();errorBox.classList.add('hidden');button.disabled=true;button.textContent='Entrando...';try{const response=await fetch('/api/auth/login',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({usuario:form.usuario.value.trim(),senha:form.senha.value})});const body=await response.json().catch(()=>({}));if(!response.ok)throw new Error(body.erro||'Não foi possível entrar.');window.location.replace('/');}catch(error){errorBox.textContent=error.message;errorBox.classList.remove('hidden');}finally{button.disabled=false;button.textContent='Entrar no Genesis';}});

const loginThemeToggle=document.querySelector('#loginThemeToggle');function syncLoginTheme(){const dark=document.documentElement.dataset.theme==='dark';loginThemeToggle.textContent=dark?'☀':'☾';loginThemeToggle.setAttribute('aria-label',dark?'Ativar modo claro':'Ativar modo escuro');}loginThemeToggle.addEventListener('click',()=>{const next=document.documentElement.dataset.theme==='dark'?'light':'dark';document.documentElement.dataset.theme=next;document.documentElement.style.colorScheme=next;localStorage.setItem('genesis_theme',next);syncLoginTheme();});syncLoginTheme();
